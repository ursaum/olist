name: Atualizar e publicar dashboard

on:
  schedule:
    - cron: "0 9 * * *"        # todo dia às 06:00 (horário de Brasília)
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: dashboard-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Buscar pedidos recentes na Shopify
        env:
          SHOPIFY_STORE_DOMAIN: ${{ secrets.SHOPIFY_STORE_DOMAIN }}
          SHOPIFY_ADMIN_TOKEN: ${{ secrets.SHOPIFY_ADMIN_TOKEN }}
        run: |
          if [ -z "$SHOPIFY_ADMIN_TOKEN" ] || [ -z "$SHOPIFY_STORE_DOMAIN" ]; then
            echo "Segredos SHOPIFY_STORE_DOMAIN / SHOPIFY_ADMIN_TOKEN não configurados; usando só os dados já salvos."
            exit 0
          fi
          python3 scripts/fetch_shopify.py --out data/shopify/api-orders.json

      - name: Salvar pedidos acumulados no repositório
        run: |
          if git diff --quiet -- data/shopify/api-orders.json && ! git ls-files --others --exclude-standard | grep -q '^data/shopify/api-orders.json$'; then
            echo "Sem pedidos novos."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/shopify/api-orders.json
          git commit -m "Atualiza pedidos da Shopify ($(date -u +%Y-%m-%d))"
          git push

      - name: Gerar dados do dashboard
        run: python3 scripts/build_shopify.py --in 'data/shopify/*.json' --out dashboard

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
